import React from 'react'

function Art() {
  return (
    <div className="artwork">
      {/* <img src={imageSrc} alt={title} />
      <h2>{title}</h2>
      <p>{description}</p> */}
    </div>
  )
}

export default Art
